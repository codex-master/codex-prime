export const host = process.env.host;
export const port = process.env.port;
export const baseURL = `http://${host}:${port}/api/v1/`;
export const fulcrumAssetURL = process.env.fulcrumAssetURL;
export const reviveAssetURL = process.env.reviveAssetURL;
export const stripePubKey = process.env.stripe_pub_key;
